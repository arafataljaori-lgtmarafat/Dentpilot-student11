# سجل التغييرات — DentPilot Student

## v1.14.0 — طبقة الجامعات لقسم الكاسشيتات
- عند الضغط على «الكاسشيتات» (من الرئيسية أو القائمة الجانبية) تظهر الآن **قائمة جامعات** أولاً بشكل بطاقات (ذمار، جينيس، السعيدة، صنعاء، الجزيرة، إب) بنفس أسلوب بطاقات الرئيسية، مع زر رجوع موحّد كبقية التطبيق.
- **جامعة الجزيرة وجامعة إب**: تفتحان نفس شاشة الكاسشيتات الحالية حرفياً (نفس القالب، نفس الحفظ، نفس معاينة/طباعة PDF) — بلا أي تكرار للكود؛ كلتاهما تُوجَّهان لنفس المسار الموجود مسبقاً.
- **ذمار، جينيس، السعيدة، صنعاء**: صفحة حالة فارغة أنيقة نصّها «سيتم إضافة كاسشيتات هذه الجامعة قريبًا» مع رجوع لقائمة الجامعات.
- تنقّل فقط: لم يتغيّر قالب الكاسشيت أو حقوله أو منطق الحفظ/الاسترجاع/PDF، ولم تتغيّر بنية Firebase أو التخزين المحلي إطلاقاً.

## v1.13.0 — خيار لغة الواجهة (عربي/إنجليزي) + إصلاح إزاحة خلفية الرئيسية
- **خيار لغة جديد**: مربع واضح أعلى صفحة الحساب (عنوان "اللغة" + خياري العربية/English) — العربية الافتراضية وتبقى دون أي تغيير مطلقاً، والإنجليزية تُترجم واجهة التطبيق الثابتة بالكامل (تسميات، حالات، أقسام، أزرار، رسائل) مع تحويل الاتجاه إلى LTR وخط لاتيني أنظف.
- **حماية صارمة للبيانات**: كل ما يكتبه الطالب بنفسه (اسم المريض، الهاتف، الملاحظات، اسم الطالب...) يبقى معروضاً حرفياً كما كُتب بغضّ النظر عن اللغة. القيم المخزَّنة فعلياً لحالة الإنجاز والقسم لا تتغيّر أبداً — تُترجَم واجهتها المعروضة فقط عبر دالتين مركزيتين آمنتين (`statusLabel`/`deptLabel`)، بينما البيانات المحفوظة في التخزين المحلي/Firebase تبقى بصيغتها الأصلية دائماً.
- اللغة تُحفظ محلياً بمفتاح مستقل تماماً عن كل مفاتيح التخزين الحالية، وتُطبَّق تلقائياً عند إعادة فتح التطبيق.
- **استُثنيت عمداً بالكامل من الترجمة**: نظام الكاسشيتات (بطلب صريح)، وصفحات الدعم/التفعيل/الدفع/الخطط (لاحتوائها أرقام حسابات بنكية وأسعار وروابط تواصل فعلية) — تبقى هذه الأقسام عربية 100% بغضّ النظر عن اللغة المختارة، مهما كانت.
- **إصلاح إزاحة خلفية الصفحة الرئيسية** المُبلَّغ عنها: كانت النسخة السابقة (1.12.5) تعتمد هامشاً سالباً لتمديد الخلفية لحواف الشاشة، وهذا تسبّب بإزاحة ظاهرة على بعض الأجهزة. الحل الجذري الآن: تبديل خلفية `body` نفسها عبر class يُفعَّله الراوتر — بلا أي هامش سالب أو حساب أبعاد إطلاقاً.
- تحقّق فعلي شامل بعد كل التعديلات: **123 فحصاً آلياً** عبر متصفح حقيقي (43 لهذه الجولة تحديداً: اللغة والترجمة والاستثناءات وإصلاح الإزاحة، بالإضافة إلى إعادة تشغيل كل مجموعات الاختبار من الجولات السابقة: 31 لنظام البطاقات الموحّد + 24 لصفحة الملف + 25 من الجولة الرابعة) — صفر كسر وظيفي، حاجز التفعيل 1.11.1 سليم، وصفحتا الدعم والنسخة الاحتياطية سليمتان تماماً.

## v1.12.5 — تحسين ألوان الصفحة الرئيسية فقط (الكروت والمسافات الفارغة)
- خلفية الصفحة الرئيسية أصبحت سماوية-مينت أعمق ومتدرّجة بدل الأبيض شبه الصافي القريب جداً من لون الكروت، ما يفصل الكروت بصرياً عن المسافة الفارغة بينها.
- حدّ أوضح على كل الكروت البيضاء في هذه الصفحة تحديداً (جميع المرضى، اليوم/الغد، المكتملة، إضافة كاسشيت، الحالات حسب المادة).
- لمسة لونية دلالية خفيفة جداً لكل بطاقة إحصائية (سماوي لجميع المرضى، فيروزي لليوم والغد، نعناعي للمكتملة) بدل الأبيض المتطابق للثلاثة.
- توحيد سطح أيام الأسبوع غير النشطة في الشريط الأسبوعي مع بقية الكروت المُحسَّنة.
- **مُقيَّد بالكامل بالصفحة الرئيسية فقط** عبر نطاق `#view-dashboard` — تحقّق فعلي (16 فحصاً) أن كل صفحة أخرى (المرضى، الحالات، الإعدادات، الدعم، النسخة الاحتياطية...) لم تتأثر بأي بكسل، وأن حاجز التفعيل 1.11.1 والوظائف الأساسية سليمة تماماً.

## v1.12.4 — تأثيرات بصرية دائمة على الكروت + إزالة اسم الجامعة من الكاسشيت + قسم المجتمعات
- تأثيرات بصرية "دائمة الظهور" وليست مرتبطة باللمس/التحويم فقط: لمعان زجاجي متحرك ببطء (~8 ثوانٍ، يحترم إعداد "تقليل الحركة") على الـHero الرئيسي ورأس ملف الحالة، ولمسة زجاجية قطرية ثابتة مدمجة داخل سطح كل بطاقات ملفات المرضى، وحدّ علوي لامع دائم على الأزرار المتدرّجة.
- إزالة اسم "University of Al-Jazeera" نهائياً من 5 مواضع: بطاقة اختيار الكاسشيت، عنوان نموذج الكاسشيت، ترويستا صفحة الطباعة/PDF (الأولى والتقييم)، وعنوان تبويب صفحة الطباعة (يظهر في ترويسة الطباعة الافتراضية بالمتصفح). يبقى عنوان الكاسشيت "ORAL SURGERY" فقط. أُضيفت ملاحظة أن قوالب إضافية قادمة في تحديث لاحق.
- ⚠️ المُعرِّف الداخلي للقالب (`jazeera-oral-surgery`) لم يتغيّر — الكاسشيتات المحفوظة مسبقاً تعمل بلا أي عطل (تم اختبار ذلك فعلياً).
- إضافة عنصر "المجتمعات" في القائمة الجانبية: رابط خارجي مباشر لمجموعة واتساب المجتمع، يفتح في تبويب جديد دون أي تأثير على حالة تنقّل التطبيق الحالية.
- تحقّق فعلي شامل بعد كل التعديلات: 80 فحصاً آلياً عبر متصفح حقيقي (25 لهذه الجولة + 31 لنظام البطاقات الموحّد + 24 لصفحة الملف الداخلية) — بلا أي كسر وظيفي، وحاجز التفعيل 1.11.1 وصفحتا الدعم والنسخة الاحتياطية سليمة تماماً.

## v1.12.3 — تطوير صفحة ملف الحالة الداخلية + رفع مستوى بطاقات المرضى الخارجية
- **إصلاح خلل حقيقي**: شبكة بيانات الحالة (`.info-grid`) كانت تُجبَر على عمود واحد على أي شاشة ≤480px بسبب قاعدة CSS قديمة زائدة — أصبحت الآن عمودين فعليين على 390/430px (والعمود الواحد يبقى فقط على الشاشات الأضيق ≤360px)، ما يقلّص طول الصفحة تقريباً للنصف.
- رأس صفحة ملف الحالة: تدرج كحلي–فيروزي أعمق بثلاث درجات مع توهج زجاجي خفيف وحلقة توهّج فيروزية حول الأفتار.
- شارة حالة الإنجاز في الرأس أصبحت بلون دلالي حسب الحالة الفعلية (كهرماني لقيد العمل، أخضر نعناعي للمكتملة، أزرق سماوي لتحتاج مراجعة، أحمر هادئ للملغاة) بدل الأبيض الثابت للجميع.
- الحقول الفارغة ("—") في بيانات الحالة أصبحت مخفَّفة بصرياً (حد متقطع، نص باهت) بدل أخذ نفس الوزن البصري للحقول الممتلئة — دون حذف أي حقل.
- تراتبية أزرار ملف الحالة: «تعديل» أصبح الإجراء الأساسي البارز بتدرج كحلي–فيروزي، و«حذف» أصغر حجماً وأهدأ لوناً — بلا أي تغيير في النص أو الحدث أو الوجهة.
- تحديث بطاقات الجلسات والملاحظات والمرفقات وشارات الحالة الأساسية (`st-wait`/`st-done`...) في كل التطبيق لتستخدم نفس اللوحة الجديدة.
- إضافة حلقة توهّج فيروزية خفيفة حول أفتار/شارات الأرقام في كل بطاقات ملفات المرضى الخارجية (جميع المرضى، الحالات، المواعيد، اليوم والغد، الكاسشيتات) للاتساق مع رأس صفحة الملف.
- تحقّق فعلي شامل (55 فحصاً آلياً عبر متصفح حقيقي: 31 لنظام البطاقات الموحّد + 24 لصفحة الملف) بلا أي كسر وظيفي: الجلسات، الملاحظات، المرفقات، التعديل، الحذف، الإنهاء، البحث، الفلاتر، حاجز التفعيل 1.11.1، وصفحتا النسخة الاحتياطية والدعم — كل ذلك يعمل كما كان تماماً.

## v1.12.2 — توحيد وترقية الشكل الخارجي لبطاقات ملفات المرضى في كل أقسام التطبيق
- إنشاء نظام بصري مشترك واحد `.dp-pfile` تستخدمه **كل** العناصر التي تفتح ملف مريض: سطح لؤلؤي سماوي متدرّج، حد رفيع مائل للفيروزي `rgba(13,115,119,.18)`، نصف قطر 20px، ظل متعدد الطبقات `0 12px 30px rgba(7,56,79,.10), 0 3px 8px rgba(0,118,111,.07)`، لمسة لونية جانبية (فيروزي→نعناعي) على حافة البداية في RTL، وحالة ضغط راقية.
- تطبيق النظام على 9 مواضع فعلية: جميع المرضى، نتائج البحث، جميع الحالات والفلاتر، الحالات المكتملة، التجميع حسب المادة، ملخص المريض، المواعيد، بطاقات اليوم والغد في الرئيسية، والكاسشيتات المحفوظة.
- اعتماد لوحة ألوان أعمق (navy-deep #07384F، navy #0A5168، teal-dark #00766F، teal #00978D، mint #2BC5A4، ice #F2FAFB، card #F7FCFC).
- ترقية زر فتح الملف الموجود إلى تدرج كحلي–فيروزي بحالة ضغط واضحة (النص والحدث والوجهة بلا تغيير)، ودمج شارات الأرقام والحقول الثانوية بدل ظهورها في فراغ غير منظم.
- إصلاح خروج زر «عرض الملفات» خارج البطاقة على الشاشات ≤360px عند اجتماع اسم طويل مع موعد طويل، وإعادة توزيع مرتبة للمحتوى على 320–430px.
- الحالات المكتملة أصبحت مقروءة بالكامل (بلا تعتيم) مع لمسة نجاح خضراء على حافة البطاقة.
- لم تُمسّ: صفحة ملف المريض الداخلية، الحساب، الدعم والتفعيل، النسخ الاحتياطي، Firebase، المزامنة، LocalStorage، الـRouter، حاجز الوصول 1.11.1، ومنطق Service Worker (تغيّر اسم الـCACHE للإصدار فقط).

## v1.12.1 — تحسين واجهة فقط (UI/UX): هوية Premium Medical سماوية–كحلية–فيروزية
- اعتماد لوحة ألوان رسمية موحّدة على مستوى المتغيّرات (Navy #073B55، Dark Navy #062C40، Teal #008F8A، Mint #2CC7A5، Background #F3F8FA، Surface #F9FCFC، Secondary #E8F4F5، Text #173B56، Muted #6B7F8B، Border #D7E5E8).
- توحيد الظل في التطبيق كله على ظل ناعم واحد `0 8px 24px rgba(23,59,86,.08)` وإزالة الظلال الثقيلة والمؤثرات الزائدة من الجولة السابقة.
- الصفحة الرئيسية: تدرج كحلي–فيروزي أهدأ للـHero مع عمق بسيط، وتحسين المسافات والمحاذاة، وأنصاف أقطار 18px للبطاقات والإحصائيات.
- الشريط السفلي: خلفية شفافة أنقى، حالة نشطة أوضح (خلفية سماوية + حد + أيقونة/نص فيروزي)، وضبط المسافات وحجم الأيقونات.
- بطاقات المرضى والحالات والمواعيد: سطح #F9FCFC وحد #D7E5E8 ونصف قطر 18px وظل خفيف، مع جعل اسم المريض أوضح عنصر (كحلي عريض) والتفاصيل أصغر بلون رمادي هادئ.
- صفحة ملف المريض: تحسين الرأس والمسافات ووضوح عناوين الأقسام وتباين التسميات/القيم، مع بقاء كل البيانات والأقسام والأزرار وترتيبها كما هي.
- لم تُمسّ: Firebase، المصادقة، المزامنة، النسخ الاحتياطي، نظام التفعيل وحاجز الوصول، مفاتيح التخزين، الـRouter، ووظائف الأزرار. تغيير `service-worker.js` اقتصر على اسم الـCACHE لرقم الإصدار فقط.

## v1.12.0 — نسخة النخبة (Elite Edition): هوية بصرية جديدة + إعادة هيكلة الإعدادات
- هوية بصرية جديدة (Elite): تدرج فيروزي ملكي عميق (#004d4d → #008080) وتأثيرات زجاجية (Glassmorphism) وظلال عميقة ناعمة على الـHero، شريط الأسبوع، أزرار الإجراء الرئيسية، وبطاقة المريض.
- إعادة تصميم بطاقة المريض: دائرة ترقيم تسلسلي (01، 02...) بدل الحرف الأول، إضافة سطر "رقم الملف"، ولوحة زجاجية مدمجة للموعد القادم مع زر "عرض الملفات" — في كل قوائم المرضى (الرئيسية، البحث).
- إعادة هيكلة صفحة الإعدادات: حذف قسم "جدول الأسبوع السريري"، وإضافة لوحة "نظرة سريعة" (نسبة الإنجاز، أكثر التخصصات عملاً، تنبيهات المتطلبات المتبقية)، وتجميع الباقي داخل أقسام قابلة للطي (التفضيلات / الدعم).
- تم إرجاع رأس صفحة تفاصيل الحالة (#file/...) حرفياً إلى تصميمه الأصلي في 1.11.1 دون أي تغيير، بحماية صارمة.
- لم تُمس صفحتا النسخة الاحتياطية والدعم/التفعيل إطلاقاً، ولم يتغيّر أي منطق حفظ بيانات أو مسار تنقّل.

## v1.11.1 — إصلاح أمني: تجاوز شاشة التفعيل الإجبارية بعد انتهاء التجربة
- إصلاح تجاوز شاشة التفعيل الإجبارية بعد انتهاء الفترة التجريبية (كان بالإمكان الوصول إلى dashboard/patients/… بمجرد إخفاء النافذة دون تفعيل فعلي).
- إضافة حاجز وصول مركزي (`enforceAccessLock()` / `isAccessLocked()` في `script.js`) يقيّد كل الصفحات والـRouter (`go`, `applyRoute`, `goBack`, `openFile`) وكل إجراءات الحفظ/التعديل/الفتح على صفحة `support` فقط أثناء انتهاء التجربة.
- إعادة التحقق من حالة التفعيل تلقائياً عند `hashchange`, `pageshow`, `focus`, `visibilitychange`, `online`, وعند انتهاء التجربة حيّاً أثناء بقاء التطبيق مفتوحاً (بلا انتظار إعادة تحميل).
- فرض القفل فوراً عند تحوّل الحالة من «تجربة» إلى «منتهية» أثناء التشغيل: إغلاق أي نافذة تشغيلية مفتوحة، إظهار شاشة التفعيل الإلزامية، ومنع أي حفظ جديد.
- تقليل وظائف التفعيل المكشوفة على `window.DPLicense` (حذف `generateCode`, `generatePlanCode`, `validateCode`, `validatePlanCode`, `activate` من التصدير) دون التأثير على الأكواد الحالية أو تنسيقها.

## v1.10.0 (2026) — تحسين بصري احترافي محدود للصفحة الرئيسية فقط (Hero Card بسنّ ثلاثي الأبعاد، شريط تقويم، شريط سفلي)
### فحص أولي (كما طُلب)
- الملف الوحيد المسؤول عن بناء اللوحة: `script.js` (IIFE واحد) + قسم `#view-dashboard` في `index.html`. CSS اللوحة محصور في قواعد `.hero-*`/`.cal-*`/`.dash-*`/`.dbb-*` بصنف/معرّف مخصوص. أزرار الإجراءات مربوطة عبر تفويض عام واحد: `document.querySelectorAll('.dash-card').forEach(...)` يستدعي `go(card.dataset.go)` — نفس الآلية أُعيد استخدامها حرفياً لأزرار الشريط السفلي الجديد دون أي كود إضافي، لضمان عدم كسر الوظائف.

### 1) Header
- تحسين بصري بحت (ظل أعمق، لمسة زجاجية أوضح على الشعار) — نفس العنوان، نفس الأيقونة (🎓)، نفس الأزرار والوظائف تماماً.

### 2) Hero Card
- استبدل بطاقة الترحيب العامة السابقة ببطاقة جديدة تعرض: اسم الطالب (أو «طالب طب الأسنان» افتراضياً)، الكلية والمستوى (أو «DentPilot Student» افتراضياً)، وسطر «إجمالي الحالات: X • المكتملة: Y» — مع سنّ ثلاثي الأبعاد (SVG بتدرج لامع + توهّج ناعم + نقطتا ضوء + حلقة شفافة خفيفة، بلا كرسي أسنان ولا ازدحام) في الطرف الأيسر وبيانات الطالب في الطرف الأيمن (RTL مضبوط). تدرج كحلي عميق ← أزرق ملكي ← تركواز داكن مع طبقة Glassmorphism خفيفة.
- **لا نافذة إعداد جديدة** — تُقرأ البيانات من `settings.studentName/level/college` الموجودة فعلاً (بدون أي حقل تخزين جديد).

### 3) شريط التقويم الأفقي
- شريط جديد تحت الـHero Card مباشرة: 7 أيام (اليوم ± 3)، اسم مختصر + رقم اليوم، اليوم الحالي مميّز بتدرج كحلي/تركواز. نص «لديك X حالات اليوم» / «لا توجد حالات اليوم» محسوب من حقل `apptDate` الموجود أصلاً على الحالات (بلا نظام مواعيد جديد وبلا أي اسم مريض أو مرض ظاهر). قابل للتمرير أفقياً بشكل ناعم داخل حدوده الخاصة فقط (لا يسبب تمريراً أفقياً على مستوى الصفحة).

### 4) كروت الإحصائيات الثلاثة
- تحسين شكل فقط (ظلال أخف، حواف مستديرة، أيقونات SVG). المنطق لم يتغيّر — كان مُصلَحاً مسبقاً في نسخة سابقة (إجمالي الحالات يشمل الكل، المكتملة فقط للمكتملة).

### 5) كروت الإجراءات
- أصبحت 4 كروت فقط في شبكة اللوحة (إضافة حالة، الحالات حسب المادة، إضافة كاسشيت، الدعم والتفعيل). «إضافة حالة» بتدرج تركوازي/أزرق طبي هو الأبرز، «إضافة كاسشيت» بتدرج أزرق ملكي-بنفسجي هادئ وأقل بروزاً، والبقية أهدأ ونظيفة. لم تتغيّر أي نصوص أو وظائف أزرار.

### 6) الشريط السفلي
- عنصر جديد داخل `#view-dashboard` فقط (لا يظهر في أي صفحة أخرى): «الرئيسية» (Active) / «نسخة احتياطية» / «الإعدادات» — نُقلا من شبكة الكروت. يعيدان استخدام نفس آلية `data-go` الحالية بلا أي منطق تنقّل جديد. حشو أسفل مخصوص بـ`#view-dashboard` فقط لمنع التغطية.

### اختبار فعلي (Puppeteer، متصفح حقيقي)
تحقّق دقيق ببيانات فعلية على 5 عروض (360/375/390/412/430px): لا تمرير أفقي، لا خروج لأي عنصر، ترتيب RTL صحيح للـHero Card، دقة نص شريط التقويم وتفاعله، حفظ حالة جديدة وكاسشيت فعلياً بنفس مفاتيح التخزين، عمل زري النسخة الاحتياطية والإعدادات من الشريط السفلي، عدم تأثر التفعيل (فحص معزول)، **ومطابقة حرفية لكل مفاتيح LocalStorage الثمانية قبل/بعد التعديل (صفر فروق)**.

> لم يتغيّر أي Form أو Modal، ولا ملف الحالة/المريض، ولا صفحة الحالات حسب المادة من الداخل، ولا الكاسشيت أو طباعته، ولا نظام التفعيل، ولا نظام النسخ الاحتياطي، ولا منطق الحفظ، ولا Backend/Supabase.

## v1.9.0 (2026) — بيانات الطالب الحقيقية، إصلاح الأرقام المتعارضة، وأيقونات SVG موحّدة
### فحص أولي (كما طُلب): خريطة المشروع
- الملف الوحيد المسؤول عن كل الواجهة والمنطق هو `script.js` (IIFE واحد). التخزين: `localStorage` بمفاتيح ثابتة (`dentpilot_student_settings_v1` وغيرها). بيانات الطالب كانت مقتصرة على `studentName` فقط (لا مستوى ولا كلية)، وبطاقة الترحيب من الجلسة السابقة لم تكن مربوطة بها إطلاقاً — هذا سبب «اختفاء الاسم».

### 1) بيانات الطالب: نافذة إعداد أولية + تعديل من الإعدادات
- نافذة جديدة (`studentSetupOverlay`) تطلب الاسم (إلزامي) + المستوى + الكلية، **تظهر تلقائياً مرة واحدة فقط** عند عدم وجود اسم محفوظ، ولا تتزاحم مع نافذة التفعيل. زر «لاحقاً» يؤجّلها دون اعتبارها محفوظة (تستمر بالظهور حتى الحفظ الفعلي).
- صفحة الإعدادات تعرض بيانات الطالب الثلاث بدل حقل الاسم المنفرد، مع زر «✏️ تعديل بيانات الطالب» يفتح **نفس النافذة** بالضبط (كما طُلب حرفياً) مملوءة بالبيانات الحالية.
- البيانات تُحفظ ضمن نفس مفتاح `dentpilot_student_settings_v1` القديم (حقلان جديدان فقط: `level`، `college`) — لا كسر لأي بيانات قديمة.
- **أُصلحت مشكلة عدم ظهور الاسم**: بطاقة الترحيب أصبحت مربوطة فعلياً بـ`settings.studentName/level/college` وتتحدّث فوراً عند كل حفظ أو عند العودة للرئيسية.

### 2) بطاقة الترحيب: محتوى حقيقي بدل النص العام
- «مرحباً، {الاسم} 👋» + سطر المستوى/الكلية (يظهر فقط إن وُجد) + رسالة ذكية مدمجة (مثال: «لديك 3 حالة إجمالاً، منها 1 مكتملة») تجمع إجمالي الحالات والمكتملة في جملة واحدة أنيقة بدل تكرار الأرقام. التاريخ العربي («الثلاثاء 21/7/2026») ما زال يتحدّث تلقائياً حسب تاريخ الجهاز.

### 3) إصلاح «الأرقام المتعارضة»
- كرت «جميع المرضى» أصبح **«إجمالي الحالات»** ويعرض فعلياً كل الحالات (مكتملة + غير مكتملة)، بعد أن كان يعرض النشطة فقط رغم اسمه. قائمة «جميع المرضى» (عند الضغط على الكرت) عُدِّلت لتطابق نفس الرقم تماماً. أُضيف نص توضيحي صغير لكل كرت إحصائي («كل الحالات» / «مكتملة فقط» / «نسبة الإنجاز»).

### 4) أيقونات SVG موحّدة بدل الإيموجي
- استُبدلت كل الإيموجي في الكروت الثلاثة العلوية والستة السفلية (وبطاقة قالب الكاسشيت المشابهة) بأيقونات SVG بسيطة موحّدة الأسلوب (stroke-based) مرسومة يدوياً داخل الملفات مباشرة — **بلا أي مكتبة أو CDN خارجي** حفاظاً على عمل التطبيق بلا إنترنت (PWA)، مع ضبط ألوانها تلقائياً حسب لون كل كرت (أبيض على الكروت الملوّنة، تركوازي/كحلي على البيضاء).

> لم يتغيّر منطق التفعيل ولا مفاتيح LocalStorage الحالية، ولم تُفقد أي بيانات حالات أو طلاب، ولم يتأثر الكاسشيت ولا النسخ الاحتياطي، ولا Backend/Supabase.

## v1.8.0 (2026) — كرت ترحيب فاخر جديد فوق الكروت الثلاثة العلوية
- كرت أفقي جديد بين شريط الفترة التجريبية والكروت الثلاثة العلوية، **خاص بشاشة اللوحة الرئيسية فقط** (لا يظهر في أي صفحة أخرى، لأنه داخل قسم اللوحة نفسه).
- تصميم فاخر شبه زجاجي: تدرج (كحلي داكن → أزرق ملكي → تركوازي) بنفس متغيرات هوية التطبيق الحالية، مع توهّج ناعم جداً وسطوع زجاجي خفيف (بلا أي صورة/مورد خارجي).
- المحتوى: «مرحباً بك 👋» + «نظّم حالاتك وطوّر مهاراتك» + سطر فرعي «متابعة ذكية لحالاتك الأكاديمية اليومية» يميناً (RTL)، وكبسولة زجاجية (`backdrop-filter`) تعرض تاريخ اليوم بالعربية (مثال: «الثلاثاء 21/7/2026») يساراً.
- الارتفاع 112px (ضمن نطاق 110–130px المطلوب)، ويحافظ على نفس الهوامش الجانبية للصفحة (18px) دون ملامسة الحواف.
- **لم يتغيّر ترتيب أو حجم أو لون أي من الكروت الثلاثة العلوية ولا الكروت الستة السفلية** — تحقّق آلي بالقياس (152px/66px كما كانت تماماً).
- تحقّق آلي: لا تمرير أفقي ولا خروج عن حواف الشاشة عند 320/360/390/414px، ولا أخطاء كونسول.

> لم يُمَسّ منطق التطبيق ولا التفعيل ولا LocalStorage ولا أي قسم آخر خارج اللوحة الرئيسية، ولا Backend/Supabase.

## v1.7.3 (2026) — زيادة إضافية واضحة لارتفاع الكروت الثلاثة العلوية فقط (CSS فقط)
- الكروت الثلاثة العلوية (متطلبات الكلية/جميع المرضى/الحالات المكتملة): الارتفاع من 130px إلى **152px** (`height` صريحة إلى جانب `min-height` لضمان ظهور الزيادة فعلياً وعدم تجاهلها)، مع تكبير تناسبي بسيط للأيقونة/الحلقة/الرقم حتى لا يبدو الكرت فارغاً.
- تحقّق آلي: الارتفاع الفعلي 152px بالضبط للكروت الثلاثة، والمحتوى متوسّط رأسياً بدقة (41px فراغ من الأعلى = 41px من الأسفل).
- **لم يتغيّر العرض ولا الترتيب ولا الألوان ولا التدرجات، والكروت الستة السفلية بقيت 66px تماماً كما كانت** — تحقّق آلي شامل. لم يتغيّر `index.html`، والتعديل الوحيد في `style.css` هو قيم صنف `.dash-stat` وعناصره الداخلية.
- لا تمرير أفقي عند 320/360/390/414px (تحقّق آلي).

## v1.7.2 (2026) — ضبط ارتفاع كروت اللوحة لتقليل الفراغ الأسفل (CSS فقط، بلا أي تغيير آخر)
- الكروت الثلاثة العلوية (متطلبات الكلية/جميع المرضى/الحالات المكتملة): الارتفاع من 112px إلى 130px (+18.8%، ضمن نطاق 15%-20% المطلوب).
- كروت الإجراءات الستة أسفلها: الارتفاع من 58px إلى 66px (+8px، ضمن نطاق 6px-10px المطلوب) — تحقّق آلي أن الكروت الستة ما زالت متساوية الارتفاع تماماً وأن كرت «إضافة حالة» لم يكبر عن البقية.
- **لم يتغيّر الترتيب ولا العرض ولا الألوان ولا التدرجات** — تحقّق آلي بمطابقة الترتيب والتدرجات حرفياً قبل/بعد. التعديل الوحيد: قيمتا `min-height`/`padding` لصنفي `.dash-card` و`.dash-stat` في `style.css`؛ لم يتغيّر `index.html` ولا `script.js` (باستثناء رقم الإصدار).
- تحقّق آلي: لا تمرير أفقي ولا أي كرت خارج الشاشة عند 320/360/390/414px.

## v1.7.1 (2026) — تحسينات بصرية إضافية للوحة الرئيسية فقط (بلا أي تغيير في الترتيب أو المنطق)
- **حجم موحّد لكل كروت الإجراءات أسفل الإحصائيات**: أُزيل التمييز في الحجم بين «الإعدادات/الدعم» وبقية الكروت (كانت أعرض/أطول) — الآن كل الكروت الستة بنفس الارتفاع والعرض تماماً (تحقّق آلي: 58px لكل الكروت).
- **كرت «إضافة حالة»**: أُصلح فقد غير مقصود لتدرجه اللوني من جلسة سابقة، وأُعطي تدرجاً تركوازياً فاخراً (135deg) — الإبراز بصري باللون فقط دون أي زيادة في الحجم (تحقّق آلي: نفس ارتفاع بقية الكروت تماماً).
- **كرت «إضافة كاسشيت»**: تدرج كحلي-تركوازي مختلف وأهدأ من كرت إضافة حالة.
- **كرت «الإعدادات»**: تدرج أزرق فاتح متناسق وواضح (غير باهت) بدل الأبيض الباهت السابق.
- **كرت «الدعم والتفعيل»**: تدرج ذهبي/برونزي فاخر جديد بدل الكحلي الغامق، مع إبقاء النص أبيض للتباين الواضح.
- **الكروت الثلاثة العلوية (متطلبات الكلية/جميع المرضى/الحالات المكتملة)**: ارتفاع أكبر، أيقونات وحلقة النسبة وأرقام أوضح وأكبر، ظل أعمق قليلاً لإحساس أفخم.
- **تحقّق آلي عند عرضين مختلفين (320px و390px)**: لا تمرير أفقي ولا أي كرت يتجاوز حواف الشاشة.
- **لم يتغيّر ترتيب أي كرت** — نفس الترتيب المنشور في v1.7.0 بالضبط (تحقّق آلي بمطابقة الترتيب حرفياً).

> لم يُمَسّ منطق التطبيق ولا التفعيل ولا LocalStorage ولا أي قسم آخر خارج الواجهة الرئيسية.

## v1.7.0 (2026) — إعادة تصميم بصري للوحة الرئيسية وقوائم المرضى وملف المريض (بلا أي تغيير في المنطق)
> صورة تصميم مرجعي أُرسلت هذه الجلسة استُخدمت لفهم فكرة التوزيع فقط (كما طُلب صراحة)، ولم تُنسخ حرفياً — تم تطبيق الأفكار داخل هوية DentPilot (كحلي/تركواز/أبيض) دون تغييرها.

### 1–2) اللوحة الرئيسية: صف إحصائيات علوي + إصلاح العرض
- صف جديد من 3 بطاقات إحصائية أعلى اللوحة: «متطلبات الكلية» (بحلقة نسبة دائرية CSS تعرض النسبة الإجمالية المحسوبة فعلياً من كل المواد ذات عدد مطلوب) — «جميع المرضى» في المنتصف — «الحالات المكتملة». باقي كروت الإجراءات في شبكة منفصلة أسفلها.
- **جميع أعمدة الشبكات الجديدة تستخدم `minmax(0,1fr)`** (نفس الإصلاح الجذري لمشكلة خروج الكروت من الشاشة الذي طُبِّق سابقاً) — تم التحقق آلياً بالقياس أن لا كرت يتجاوز حدود الشاشة ولا يوجد تمرير أفقي.

### 3) ترتيب الكروت
- «إضافة حالة» أصبح أول كرت في شبكة الإجراءات (في المساحة التي كانت لكرت الكاسشيت)، و«الحالات حسب المادة» بجانبه مباشرة. «الإعدادات» و«الدعم والتفعيل» أصبحا كرتين عريضين (يمتدان بعرض الصف كاملاً) بتصميم مميّز واضح عن باقي الكروت.

### 4–6) توحيد شكل شريط المريض في كل الأقسام
- زر «📁 فتح ملف المريض» ظل كبسولة تركوازية واضحة كما في الجلسة السابقة، وأُضيف بجانبه زر سريع «✅ إنهاء» يظهر فقط للحالات غير المكتملة (البند 7) — يتيح إنهاء الحالة مباشرة من القائمة دون فتح الملف أو الدخول لوضع التعديل، بنفس تأكيد الحفظ الآمن المستخدم سابقاً. الشكل موحّد في جميع المرضى، الحالات المكتملة، وشاشة حالات كل مادة («الحالات حسب المادة» ما زالت تعرض المواد كشرائط أولاً، ثم حالات كل مادة بالشكل المضغوط).

### 5) تحسين مظهر ملف المريض
- تحسينات بصرية بحتة (بلا أي تغيير في البيانات المعروضة أو منطق الحفظ): عمق أوضح لبطاقة رأس الملف، وتبسيط شبكة البيانات إلى تخطيط أنظف بخطوط فاصلة بدل صناديق مزدحمة.

### 8) صفحة الدعم والتفعيل بعد التفعيل
- أُضيف كرت وكيل طلاب محافظة إب (بنفس التصميم الأفقي المضغوط المستخدم أصلاً في الحالة السابقة للتفعيل) أسفل الكرت الرسمي «✅ التطبيق مفعل بنجاح»، مع فاصل ومسافة واضحة بينهما وزر واتساب مباشر.

> لم يتغيّر منطق التطبيق ولا نظام التفعيل ولا مفاتيح LocalStorage الحالية، ولم تُفقد أي بيانات حالات قديمة.

## v1.6.0 (2026) — إصلاح جذري لخروج كروت اللوحة من الشاشة، شريط مريض موحّد ومضغوط، شرائط المواد، وزر إنهاء الحالة
### 1) إصلاح عرض كروت اللوحة الرئيسية (السبب الجذري)
- شُخِّصت المشكلة فعلياً: أعمدة `grid-template-columns:repeat(2,1fr)` كانت تتمدّد حسب محتوى الكرت الداخلي (flex-row) بلا حدّ أقصى، ما يجعل عمود كرت "إضافة حالة" يبدأ عند إحداثي **سالب** (خارج حافة الشاشة تماماً) بينما يتمدّد العمود الآخر بعرض مختلف تماماً. الإصلاح: `minmax(0,1fr)` على العمودين (والشبكة الثلاثية للشاشات الأوسع) مع `min-width:0` على الكرت نفسه. **تحقّق فعلي بالقياس:** كل الكروت الآن بعرض متساوٍ تماماً (159px/عمود) وداخل حدود الشاشة بلا أي تمرير أفقي.

### 2) زر «✅ إنهاء الحالة» داخل ملف المريض
- يظهر فقط للحالات غير المكتملة، مع تأكيد بسيط قبل التنفيذ. بعد التأكيد تتحوّل الحالة إلى «مكتملة» فوراً دون حذفها، وتبقى ظاهرة في قسم الحالات المكتملة (قابلة للإرجاع من هناك كما كان).

### 3) زر «📁 فتح ملف المريض» بدل أيقونة المجلد المجرّدة
- كبسولة تركوازية واضحة بنص كامل بدل الأيقونة وحدها، بنفس وظيفة الفتح الحالية تماماً.

### 4–5) شريط مريض أفقي مضغوط وموحّد في كل الأقسام
- أُعيد تصميم شكل عرض كل حالة/مريض داخل القوائم بالكامل (جميع المرضى، الحالات المكتملة، حالات كل مادة): رقم صغير + اسم واضح + شارة المادة + شارة حالة صغيرة + زر «📁 فتح ملف المريض» — بلا تفاصيل الموعد أو أسطر إضافية طويلة (تبقى هذه التفاصيل داخل ملف المريض فقط كما كانت). قائمة الحالات المكتملة تحتفظ بزر «↩︎ إرجاع».

### 6–9) إعادة تصميم «الحالات حسب المادة»
- عند فتح القسم تظهر المواد أولاً كشرائط مختصرة (اسم المادة بارز مع شريط جانبي مميّز + عدد الحالات + زر «عرض الحالات ›»)، وليس المرضى مباشرة. الضغط على مادة يفتح شاشة مستقلة جديدة («حالات Endo» مثلاً) تعرض حالات تلك المادة فقط بنفس الشريط الأفقي المضغوط، مع مسار رجوع صحيح إلى شاشة المادة نفسها (وليس القائمة العامة).

> لم يتغيّر نظام التفعيل ولا مفاتيح LocalStorage القديمة ولا منطق حفظ الحالات، ولم تُحذف أي حالة قديمة، ولم تُكسر الكاسشيت ولا متطلبات الكلية، ولم تُعدَّل الواجهة الرئيسية إلا في نقطة عرض الكروت كما طُلب تحديداً.

## v1.5.3 (2026) — إزالة «حسب الأيام»، مادة Operative، نموذج حالة مضغوط، وحقول Endo
### 1) حذف قسم «المرضى حسب الأيام»
- أُزيل كرت «المرضى حسب الأيام» من اللوحة الرئيسية، وأُزيلت شاشتا «ترتيب الحالات حسب الأيام» و«اليوم الواحد» بالكامل من التطبيق (HTML/JS/CSS). **لم تُحذف أي بيانات حالات** — حقل اليوم لا يزال محفوظاً على كل حالة. بقية الأقسام (جميع المرضى، الحالات حسب المادة، الحالات المكتملة، متطلبات الكلية، إضافة حالة، الكاسشيت) تعمل كما هي.

### 2) تغيير اسم المادة «حشوات» → Operative
- المادة الأولى أصبحت **Operative** مع وصف صغير «حشوات وترميمات»، وتظهر بالاسم الجديد في نموذج إضافة الحالة، الحالات حسب المادة، ومتطلبات الكلية. أي حالة قديمة كانت محفوظة باسم «حشوات» تُرحَّل تلقائياً إلى Operative (وكذلك «عصب» → Endo) **دون فقدان أي حالة**، ويُثبَّت الترحيل في التخزين مرة واحدة.

### 3) نموذج إضافة الحالة أصبح Compact
- تخطيط مرتّب للجوال: الحقول القصيرة مصفوفة جنباً إلى جنب (الهاتف + حالة الحالة، المادة + نوع الحالة، السن + اليوم، التاريخ + الوقت)، والحقول الطويلة (اسم المريض، الملاحظات) بعرض كامل. قُلّل ارتفاع الحقول والمسافات الداخلية مع الحفاظ على الوضوح. لم تتغيّر وظيفة الحفظ.

### 4) حقول Endo (تظهر فقط عند اختيار مادة Endo)
- قسم **Endo Details** يظهر حصرياً عند اختيار Endo: Basic Endo (Tooth No / Diagnosis / Periapical status / Visit Stage)، وبطاقات قنوات مضغوطة مع زر «+ إضافة قناة»؛ كل قناة تحتوي فقط: Canal name / WL / Stop / Initial File / Master File. يمكن إضافة عدة قنوات وحذف أي قناة. أسماء القنوات كقائمة سريعة (Single/MB/DB/ML/DL/Palatal/Distal/Mesial/Buccal/Lingual/Other). **لم تُضف أي حقول Endo أخرى** (لا Irrigation ولا Medication ولا Obturation ولا Sealer ولا Endo notes).

### 5) خيار Other / أخرى
- عند اختيار «أخرى» في مادة الحالة، أو «Other» في اسم القناة، يظهر حقل نصي صغير، وتُحفظ **القيمة المكتوبة** بدل الكلمة «أخرى/Other»، وتظهر القيمة المخصّصة محفوظة عند إعادة فتح الحالة. القيم القديمة لا تنكسر.

### 6) التخزين والتوافق
- بيانات Endo تُحفظ داخل كائن الحالة نفسه (`case.endo`) بطريقة آمنة. الحالات القديمة التي لا تحتوي Endo تفتح دون أخطاء، وحقول Endo لا تظهر عندما تكون المادة ليست Endo. لم تتغيّر مفاتيح LocalStorage القديمة.

> لم يُمَسّ نظام التفعيل ولا مفاتيح LocalStorage الحالية، ولم تُكسر متطلبات الكلية ولا الكاسشيت، ولا Backend/Supabase.

## v1.5.2 (2026) — طباعة الكاسشيت عبر صفحة قالب مستقلة تماماً (case-sheet-print.html)
### السبب
- رغم إصلاح v1.5.1، ظلّت بعض متصفحات الجوال تتجاهل قواعد `@media print` فوق واجهة التطبيق عند «مشاركة → طباعة/PDF»، فتُطبع شاشة التطبيق نفسها (شريط التجربة، زر التثبيت، الشعار، حقول الإدخال).

### الحل الجذري — كما طُلب حرفياً
- **لم نعد نطبع DOM التطبيق إطلاقاً ولا نستدعي `window.print()` على صفحة التطبيق ولا نعتمد على `@media print` فوق الواجهة.**
- ملف جديد مستقل تماماً: **`case-sheet-print.html`** (case-sheet-print-template) — صفحة ورقية A4 بيضاء بالكامل لا تحتوي أصلاً على أي عنصر من واجهة التطبيق: لا شريط تجربة، لا شعار DentPilot، لا زر تثبيت، لا بطاقات، لا ظلال، لا input/select/dropdown — نصوص وخطوط تعبئة ومربعات اختيار ☑/☐ فقط، بترتيب مطابق لملف الجامعة الأصلي (الترويسة، ORAL SURGERY، بيانات الطالب، المريض، Medical/Dental History، الفحوصات، الأشعة، التشخيص، الإجراء، تعليمات ما بعد العملية، ملاحظات المشرف).
- «معاينة الكاسشيت للطباعة» و«طباعة / تصدير PDF» يفتحان هذه الصفحة فقط (الطباعة بوضع `?print=1` تستدعي الطباعة تلقائياً **من داخل صفحة القالب نفسها**)، وتُمرَّر بيانات الكاسشيت إليها عبر مفتاح تسليم مؤقت جديد (`dentpilot_student_cs_print_v1`) دون المساس بأي مفتاح قديم.
- ضبط فواصل الصفحات: `@page { size:A4; margin:12mm }`، الأقسام `no-break`، **جدول التقييم الفارغ + توقيعا المشرف في صفحة مستقلة**، و**صفحة الصور آخر صفحة** كما في الكاسشيت الورقي. جدول التقييم ما يزال حصرياً في الطباعة ولا يظهر في شاشة التعبئة.
- أُزيلت آلية v1.5.1 القديمة بالكامل (حاوية `#csPrintRoot` وقواعدها) من script.js وindex.html وstyle.css.
- أُضيف `case-sheet-print.html` إلى كاش الـ service worker ليعمل بلا إنترنت.
- **تحقق نهائي حقيقي:** وُلِّد ملف PDF فعلي من صفحة القالب (4 صفحات A4) وفُحص بصرياً: ورقة كاسشيت رسمية نظيفة 100% بلا أي أثر لواجهة التطبيق.

> شاشة التعبئة داخل التطبيق لم تتغيّر. لم يُمَسّ نظام التفعيل ولا مفاتيح LocalStorage القديمة، ولم تُكسر الحالات القديمة ولا إضافة حالة ولا متطلبات الكلية، ولا Backend/Supabase.

## v1.5.1 (2026) — إصلاح طباعة الكاسشيت: قالب ورقي مستقل تماماً عن واجهة التطبيق
### السبب الجذري
- كانت الطباعة تعتمد على iframe مخفي خارج الشاشة (نفس أسلوب طباعة الحالات)، وهي آلية لا تعمل بشكل موثوق على بعض متصفحات الجوال (خصوصاً أندرويد) — إذ يطبع المتصفح أحياناً **الصفحة الظاهرة فعلياً** بدل محتوى الـ iframe المخفي، فتظهر واجهة التطبيق نفسها (شريط التجربة، شعار DentPilot، حقول الإدخال، القوائم المنسدلة) في نسخة الطباعة بدل القالب الورقي.

### الإصلاح
- استُبدلت الآلية بالكامل بحاوية طباعة `#csPrintRoot` داخل الصفحة نفسها (عنصر مستقل مباشر تحت `body`)، مع قواعد `@media print` صريحة في `style.css`: **تُخفي كل عناصر واجهة التطبيق دون استثناء** (الهيدر، شريط التجربة، شريط التثبيت، اللوحة، كل النوافذ والنماذج) ولا تُظهر إلا محتوى `#csPrintRoot`. هذه الطريقة موثوقة عبر جميع المتصفحات لأن `window.print()` يُستدعى على النافذة الرئيسية نفسها مباشرة، لا على نافذة iframe منفصلة.
- تم التحقق فعلياً عبر محاكاة وضع الطباعة (`@media print`) أن: شريط التجربة، الهيدر، شريط التثبيت، و#app بكل حقوله (input/select) تختفي تماماً، ولا يظهر إلا القالب الورقي.

### تحسينات شكل القالب الورقي (بالرجوع إلى ترتيب ملف الجامعة الأصلي دون تعديله)
- عرض الحقول كنصوص/أسطر فارغة بدل صناديق إدخال، واستخدام مربعات اختيار ☑/☐ فعلية لكل الحقول من نوع نعم/لا والاختيارات المتعددة (بدل نص عادي)، مطابقةً لمظهر النموذج الورقي الأصلي.
- تقسيم المحتوى إلى أقسام مع `page-break-inside:avoid` لمنع تقطيع الأقسام بشكل سيئ بين الصفحات.
- **جدول تقييم المشرف** يبدأ صفحة جديدة دائماً (`page-break-before`) ويبقى حصرياً في نسخة الطباعة/المعاينة فقط (لا يظهر في شاشة التعبئة إطلاقاً)، فارغاً تماماً ليعبّئه المشرف يدوياً، مع سطري توقيع فارغين دون أي تعبئة تلقائية.
- **قسم الصور أصبح دائماً آخر صفحة** في المستند المطبوع، مطابقةً لترتيب الكاسشيت الورقي الأصلي.
- تبويب «معاينة» يعرض بالضبط نفس محتوى وتنسيق ما سيُطبع.

> لم تتأثر شاشة تعبئة الكاسشيت داخل التطبيق (بقيت كما هي). لم يُمَسّ نظام التفعيل ولا مفاتيح LocalStorage الحالية، ولم تُكسر أي حالة أو متطلب كلية أو بيانات طلاب قديمة، ولم تُضَف أي خدمة خلفية (Backend/Supabase).

## v1.5.0 (2026) — كاسشيتات التسليم (جامعة الجزيرة — Oral Surgery) وإعادة ضبط اللوحة لتكون مضغوطة فعلياً
### 1) كاسشيتات التسليم — ميزة جديدة مستقلة تماماً عن نظام الحالات
- كرت جديد «إضافة كاسشيت» في اللوحة الرئيسية (بجانب «إضافة حالة»، بنفس لونه بالضبط)، يفتح شاشة اختيار قالب — يتوفر حالياً قالب واحد: «جامعة الجزيرة — Oral Surgery»، مبني بالرجوع إلى ترتيب حقول ملف الـ PDF المرفق (دون تعديل الملف الأصلي نفسه بأي شكل).
- نموذج تعبئة كامل (بيانات عامة، بيانات المريض، التاريخ الطبي والسني، الفحص خارج/داخل الفم، الأشعة، التشخيص النهائي، الإجراء المخطَّط، تعليمات ما بعد العملية، ملاحظات المشرف، و4 خانات صور: صورتان قبل العملية + أثناء العملية + الأشعة).
- ثلاثة أزرار: «حفظ الكاسشيت» (يتطلب اسم المريض على الأقل)، «معاينة» (تفتح نسخة الطباعة في تبويب جديد)، «طباعة / تصدير PDF» (نفس آلية طباعة الحالات المعتمدة أصلاً في التطبيق — تتيح «حفظ كـ PDF» من نافذة طباعة المتصفح).
- **جدول تقييم المشرف يظهر حصرياً في نسخة الطباعة/المعاينة ولا يظهر إطلاقاً في شاشة التعبئة** — فارغ بالكامل ليعبّئه المشرف يدوياً (History & Diagnosis / Clinical & Radio Exam / Surgical Skills / Documentation / Patients Management / Total)، مع سطري توقيع فارغين («Supervisor's Name» و«Supervisor's Signature») دون أي تعبئة تلقائية.
- تُحفظ الكاسشيتات (والصور) محلياً بمفتاح مستقل جديد كلياً (`dentpilot_student_casesheets_v1`) لا علاقة له بمفاتيح الحالات أو الطلاب الحاليين، وتبقى محفوظة بعد إغلاق التطبيق وإعادة فتحه، وتُضمَّن أيضاً في النسخة الاحتياطية (تصدير/استيراد).

### 2) إعادة ضبط كروت اللوحة الرئيسية لتكون مضغوطة فعلياً
- أُعيد بناء تخطيط الكروت بالكامل إلى صفوف أفقية (الأيقونة يساراً/يميناً والنص بجانبها) بدل التكديس العمودي السابق — بأسلوب أقرب لتطبيقات العيادات، وارتفاع أقل بشكل حقيقي وملموس، مع بقاء التصميم واضحاً وأنيقاً على الجوال.

> لم يتغيّر نظام التفعيل ولا مفاتيح LocalStorage الحالية، ولم تُحذف أو تُعدَّل أي بيانات حالات/طلاب قديمة، ولم تُضَف أي خدمة خلفية (Backend/Supabase).

## v1.4.0 (2026) — متطلبات الكلية القابلة للتوسيع، ظهور الحالات المكتملة في الترتيبات، تصغير اللوحة، وزر تثبيت أنيق
### 1) قسم متطلبات الكلية
- إمكانية جديدة: زر «+ إضافة مادة» داخل قسم متطلبات الكلية يتيح للطالب إضافة مادة/متطلب باسم من اختياره.
- المادة الجديدة تُخزَّن في مفتاح محلي مستقل (`dentpilot_student_custom_reqs_v1`) ولا علاقة له بقائمة الأقسام في نموذج إضافة الحالة — فلا تتأثر قوائم إضافة الحالة إطلاقاً.
- المادة الجديدة تستخدم نفس نظام «مطلوب/مكتمل/متبقي» تماماً مثل المواد الأصلية، وتبقى محفوظة بعد إغلاق التطبيق وإعادة فتحه، وتُضمَّن أيضاً في النسخة الاحتياطية (تصدير/استيراد).
- إمكانية حذف مادة مخصّصة (مع تأكيد) دون التأثير على المواد السبعة الأصلية.

### 2) ظهور الحالات المكتملة في الترتيبات
- الحالات المكتملة لم تعد تُستبعد من «ترتيب الحالات حسب الأيام» و«ترتيب الحالات حسب المادة» — تظهر الآن بعلامة واضحة «✅ مكتملة» بجانب الحالات النشطة، مع بقائها في أرشيف «الحالات المكتملة» الخاص بها كما كان.

### 3) تصغير كروت اللوحة الرئيسية
- كروت أصغر وأكثر تناسقاً (حشو وارتفاع وأيقونات أصغر قليلاً) لعرض أكبر قدر من اللوحة في شاشة واحدة، دون أي تغيير في الألوان أو الوظائف.

### 4) شريط تثبيت التطبيق
- شريط أنيق بشكل كبسولة بلون فاتح يظهر بين الهيدر واللوحة الرئيسية فقط عندما يكون التطبيق قابلاً للتثبيت وغير مثبت بعد، ويختفي تلقائياً بعد التثبيت.
- عند الضغط: يفتح نافذة التثبيت الرسمية للمتصفح إن كانت مدعومة، وإلا يعرض تلميحاً بسيطاً وغير مزعج (توست) بطريقة الإضافة اليدوية للشاشة الرئيسية.
- تمت إضافة نفس الإمكانية أيضاً داخل صفحة الإعدادات لمن يريد الرجوع إليها لاحقاً.

> لم يتغيّر نظام التفعيل ولا مفاتيح LocalStorage الحالية ولا التخزين الحالي، ولم تُحذف أو تُعدَّل أي بيانات طلاب قديمة.

## v1.2.6 (2026) — إصلاح طارئ حاسم: أزرار ملف الحالة لا تعمل إلا بعد الضغط على «تعديل»
### السبب الجذري الحقيقي (بنية HTML خاطئة)
- تبيّن — بعد اختبار باللمس الحقيقي على متصفح فعلي (وليس بقراءة الكود فقط) — أن نوافذ **التأكيد** و**الجلسة** و**معاينة المرفق** و**التحديث** كانت جميعها **متداخلة داخل نافذة تعديل الحالة (caseOverlay) المخفية**، بدل أن تكون نوافذ مستقلة في مستوى الصفحة. السبب: وسم `<div>` مكرَّر وغير مغلق داخل نموذج الحالة (سطر `grid-2` زائد) ابتلع وسم الإغلاق الخاص بنافذة التعديل.
- النتيجة المطابقة تماماً للعطل المُبلَّغ عنه: عند الضغط على «إضافة جلسة» أو «حذف» أو «فتح المرفق»… كان الإجراء يُنفَّذ فعلاً وتُفتح النافذة (`hidden=false`)، لكنها تبقى **غير مرئية** لأنها داخل نافذة التعديل المخفية (‏`display:none`) — فيبدو أن «لا شيء يحدث». وعند الضغط لاحقاً على قلم **«تعديل»** تظهر نافذة التعديل ومعها النافذة المتداخلة التي فُتحت سابقاً — فيبدو أن «الإجراء السابق يُنفَّذ فجأة».

### الإصلاح
- **تصحيح بنية HTML**: إزالة الوسم الزائد بحيث تصبح كل النوافذ (تعديل، جلسة، تأكيد، معاينة مرفق، تحديث، تفعيل) عناصر مستقلة مباشرة في مستوى الصفحة. تم التحقّق برمجياً من أن النوافذ الست جميعها أصبحت أبناءً مباشرين لـ `body`، وأن نافذة التأكيد تُرسم بحجم كامل وزرّها ظاهر من **أول لمسة**.
- **إعادة بناء نظام النقر لملف الحالة**: تفويض أحداث موحّد بمستمع نقر واحد على جذر التطبيق `#app` عبر `closest('[data-act]')`، مع دالة `runAction` واحدة تُنفِّذ الإجراء من الزر المضغوط نفسه — لا إجراءات مؤجَّلة أو مُخزَّنة، ولا مستمعات مكرَّرة، ولا تنفيذ مزدوج. الأزرار تستدعي `preventDefault()` فقط دون الروابط (`tel:`/`wa.me` تبقى تعمل).
- **إظهار فوري للنوافذ**: دالّتا `showOverlay`/`hideOverlay` تفرضان إعادة رسم فورية عند فتح أي نافذة، تحسّباً لأي تأجيل رسم على بعض متصفحات الأجهزة.

### تم التحقق منه باللمس الحقيقي (أول لمسة، دون الضغط على «تعديل»)
- «إضافة جلسة» تفتح نافذة الجلسة فوراً · «حذف الحالة» يُظهر «هل تريد حذف هذه الحالة؟» فوراً · «حذف جلسة» يُظهر «هل تريد حذف هذه الجلسة؟» فوراً · «فتح المرفق» يفتح فوراً · «حذف المرفق» يُظهر «هل تريد حذف هذا المرفق؟» فوراً · «إرجاع إلى الحالات الحالية» يعمل فوراً · «الطباعة» تُنتج تقريراً حقيقياً · والضغط على «تعديل» يفتح نافذة التعديل فقط ولا يُنفِّذ أي إجراء سابق.

> لم تُمَسّ ملفات التصميم (style.css) ولا التفعيل (activation.js) في هذا الإصدار، ولم تُحذف أو تُصفَّر أي بيانات محلية أو تفعيل أو تجربة.

## v1.2.5 (2026) — إصلاح طارئ: استقرار الأزرار والإجراءات
### تدقيق شامل لكل الأزرار والإجراءات القابلة للنقر
- **السبب الجذري المكتشف:** كانت `renderFile()` تُعيد ربط مُستمع نقر جديد على زر «حفظ الملاحظات» في كل مرة يُعاد فيها رسم صفحة ملف الحالة (وهو ما يحدث بعد كل إجراء تقريباً داخل الملف: إضافة/حذف جلسة، حذف مرفق...)، دون إزالة المستمعات القديمة أولاً — فتتراكم المستمعات وقد يُنفَّذ الحفظ أكثر من مرة لكل نقرة واحدة.
- **الحل:** إزالة الربط المباشر نهائياً، وتحويل «حفظ الملاحظات» إلى تفويض أحداث آمن عبر `data-act="save-notes"` من جذر التطبيق (نفس نمط بقية الأزرار)، بحيث تُربط كل المستمعات مرة واحدة فقط عند بدء التشغيل.
- تم تدقيق **كل** أزرار/بطاقات/روابط التطبيق: التنقّل بين الأقسام، حالة، جلسة، مرفق، نسخة احتياطية، إعدادات، دعم وتفعيل — والتأكد من أن كل زر مربوط بمعالج فعلي وأنه لا توجد مستمعات مكرَّرة في أي مكان آخر بالتطبيق.
- تصحيح نص تأكيد **حذف الحالة** ليطابق المطلوب تماماً: «هل تريد حذف هذه الحالة؟» (كان نصاً مختلفاً سابقاً).
- إضافة `type="button"` لجميع الأزرار الثابتة المتبقية في الصفحة (بطاقات اللوحة، زر الرجوع، زر التثبيت، أزرار إغلاق النوافذ...) كإجراء وقائي إضافي، مع إبقاء زرَي الحفظ داخل النموذجين `type="submit"` كما هو مطلوب.

### تأكيد السلوك الصحيح (بعد التدقيق، دون تغيير في المنطق)
- إضافة/حذف جلسة، فتح/حذف مرفق، الطباعة، إرجاع حالة من الأرشيف (يعرض تأكيداً فورياً كما صُمم أصلاً)، التنقّل بين كل الأقسام، النسخ الاحتياطي (تصدير/استيراد)، الدعم والتفعيل (نسخ الأرقام، أزرار واتساب، تفعيل التطبيق الآن) — جميعها فعّالة فوراً دون أي تأخير أو إجراءات مؤجّلة.

> لم تُمَسّ منطق التفعيل أو التجربة (activation.js دون أي تغيير)، ولم تُحذف أو تُصفَّر أي بيانات محلية.

## v1.2.4 (2026) — إصلاح عاجل: رابط واتساب داخل ملف الحالة + تفعيل فوري
### إصلاح: ظهور رابط واتساب الخام داخل ملف الحالة
- كان رابط `wa.me` الخاص بالمريض (مع الرسالة المُرمّزة `%D8…`) يظهر كنص مرئي داخل صفحة ملف الحالة ويُسبّب تمدّداً أفقياً وفراغاً كبيراً على الجوال. السبب: تعارض في تسمية دالة `waLink` بين زر واتساب المريض ورابط دعم واتساب المُضاف في 1.2.3.
- الحل: إعادة تسمية دالة دعم واتساب إلى `waSupportLink` لإزالة التعارض، فعاد شريط أزرار ملف الحالة كما كان: **اتصال · واتساب · تعديل · طباعة · حذف**.
- زر **واتساب** يفتح محادثة المريض، والرابط الكامل موجود داخل `href` فقط ولا يظهر كنص إطلاقاً. أزرار دعم واتساب تعرض أرقاماً مختصرة فقط كذلك.
- حماية إضافية للتخطيط: `overflow-x:hidden` على الصفحة، ولفّ النصوص الطويلة (`overflow-wrap`/`word-break`).

### جديد: «تفعيل التطبيق الآن» داخل «الدعم والتفعيل»
- عند عدم التفعيل، زر واضح **«تفعيل التطبيق الآن»** يفتح شاشة التفعيل مباشرةً أثناء الفترة التجريبية دون انتظار انتهائها، مع نص: «يمكنك تفعيل التطبيق الآن دون انتظار انتهاء الفترة التجريبية.»
- من الشاشة يمكن نسخ «رمز التطبيق الخاص بهذا الهاتف» ولصق كود التفعيل ثم الضغط «تفعيل التطبيق». زر **«لاحقاً»** يغلق الشاشة أثناء التجربة (ويبقى التفعيل إلزامياً بعد انتهاء التجربة).
- لم تتغيّر معلومات الدعم/الدفع: السعر 4000 ريال، الكريمي 3015367236، جيب 25910، وأزرار واتساب 775101518 و779449744. دون تحقّق دفع تلقائي ودون نظام أكواد شراء.

> لم تُمَسّ منطق التفعيل/التجربة (activation.js دون تغيير)، ولم تُحذف أو تُصفَّر أي بيانات محلية.

## v1.2.3 (2026) — قسم «الدعم والتفعيل» + تحسين شاشة التفعيل
### جديد: قسم «الدعم والتفعيل»
- بطاقة جديدة في اللوحة الرئيسية **«الدعم والتفعيل»** بلون مميّز، ويتغيّر وصفها حسب حالة التفعيل (قبل التفعيل: «ادخل هنا لتفعيل التطبيق أو التواصل مع الدعم» — بعد التفعيل: «التطبيق مفعل — تواصل مع الدعم عند الحاجة»).
- صفحة **«الدعم والتفعيل»** (وليست «الدفع») تعرض حسب الحالة:
  - **غير مفعّل/تجريبي:** حالة التجربة (سارية/منتهية مع الوقت المتبقّي)، ملاحظة الشراء لمرة واحدة، **سعر نسخة الطلاب 4000 ريال**، خطوات التفعيل، طرق الدفع (**بنك الكريمي 3015367236** و**محفظة جيب 25910** مع زر نسخ لكل رقم)، أزرار واتساب للدعم، وبطاقة المطوّر.
  - **مفعّل:** «✅ التطبيق مفعل بنجاح»، النسخة، حالة التفعيل، رمز التطبيق، بيانات المطوّر وأزرار الدعم فقط (تُخفى الأسعار وطرق الدفع ورسائل التجربة).
- زر النسخ ينسخ الرقم مع رسالة «تم نسخ الرقم بنجاح» (أو «انسخ الرقم يدوياً» عند تعذّر النسخ).
- رسالة واتساب المُعبّأة تتضمّن اسم التطبيق ورمز التطبيق وحالة التفعيل فقط — **دون أي بيانات مرضى أو حالات أو مرفقات أو ملاحظات**.
- إضافة اختصار «الدعم والتفعيل» وبطاقة المطوّر داخل الإعدادات.

### تحسين: شاشة التفعيل
- استبدال «معرّف الجهاز» بـ **«رمز التطبيق الخاص بهذا الهاتف»** مع رسالة طمأنة بأن الرمز لا يحتوي على صور أو بيانات المرضى أو معلومات شخصية.
- خطوات أوضح: (1) نسخ رمز التطبيق، (2) لصق كود التفعيل الذي يصل من الدعم، مع نص إرشادي وأزرار واتساب داخل الشاشة.

> لم تُمَسّ منطق التفعيل أو التجربة، ولم تُحذف أو تُصفَّر أي بيانات محلية.

## v1.2.2 (2026) — إصلاحات داخل ملف الحالة (استجابة فورية + الطباعة)
### إصلاح: الأزرار داخل ملف الحالة تعمل فور اللمسة الأولى
- السبب: تأخير النقر على الجوال بسبب انتظار المتصفح لنقرة ثانية (تكبير بالنقر المزدوج)، ما جعل الإجراء يبدو وكأنه «لا يحدث» ثم ينفَّذ لاحقاً عند الضغط على زر آخر (مثل «تعديل»).
- الحل: تفعيل `touch-action: manipulation` على جميع الأزرار والعناصر القابلة للنقر، وجعل كل الأزرار المُولّدة `type="button"`، مع الاعتماد على تفويض أحداث آمن.
- النتيجة: **«فتح المرفق»** يفتح المرفق فوراً، و**«حذف المرفق»** يُظهر التأكيد فوراً «هل تريد حذف هذا المرفق؟»، و**«حذف جلسة»** يُظهر التأكيد فوراً «هل تريد حذف هذه الجلسة؟» — دون إجراءات مؤجّلة ودون تأكيدات مكرّرة.

### إصلاح: زر الطباعة كان يفتح صفحة بيضاء
- أُعيدت الطباعة لتُنشئ **تقريراً حقيقياً** داخل إطار مستقل (iframe) بدل إخفاء الصفحة، فلا يتنقّل التطبيق إلى صفحة بيضاء.
- التقرير يتضمّن: DentPilot Student، اسم الحالة، الهاتف (إن وُجد)، المادة/القسم، نوع الحالة، تاريخ ووقت الموعد، الحالة، الجلسات، الملاحظات، وأسماء المرفقات فقط، مع تذييل: «تم إنشاء التقرير بواسطة DentPilot Student».
- عند تعذّر التجهيز تظهر رسالة: «تعذر تجهيز ملف الطباعة. حاول مرة أخرى.»

> لم تُمسّ البيانات المحلية ولا التفعيل/التجربة.

## v1.2.1 (2026) — الحالات حسب المادة + تحسين موثوقية المرفقات
### جديد: الحالات حسب المادة
- استُبدل اختصار **«المرفقات»** في اللوحة الرئيسية بصفحة **«الحالات حسب المادة»** 📚.
- تُجمَّع الحالات الحالية حسب **المادة/القسم**، وتظهر الحالات بلا مادة تحت **«بدون مادة محددة»**.
- كل مجموعة تعرض عدد حالاتها وصفوفاً مضغوطة مع زر **فتح ملف الحالة**. (الحالات المكتملة تبقى في أرشيفها ولا تظهر هنا.)

### تحسين: موثوقية المرفقات على الجوال
- **المرفقات تبقى داخل ملف كل حالة كما هي** — لم يُحذف شيء.
- تحسين فتح **PDF وباقي الملفات**: يتم الآن عبر رابط Blob داخل نقرة المستخدم مباشرةً (أكثر موثوقية على الجوال من فتح نافذة منبثقة قد يحجبها المتصفح)، مع تنزيل احتياطي.
- **الصور** تُفتح في نافذة المعاينة، و**زر الحذف 🗑️** يحذف المرفق من الحالة الصحيحة فقط ويُحدِّث التخزين فوراً.
- المرفقات القديمة بلا بيانات كافية تعرض: «هذا المرفق قديم ولا يحتوي على بيانات كافية للفتح.»
- لم تُمسّ بيانات الحالات أو المرفقات أو التفعيل/التجربة.

## v1.2.0 (2026) — أرشيف الحالات المكتملة + إصلاح فتح المرفقات
### جديد: الحالات المكتملة
- قسم جديد **«الحالات المكتملة»** في اللوحة الرئيسية يعرض الحالات ذات الحالة **«مكتملة»**.
- الحالات المكتملة **تُؤرشَف** ولم تعد تظهر في القوائم الحالية («جميع المرضى» و«المرضى حسب الأيام»)، دون حذفها أو تكرارها.
- تُحفَظ **كل بيانات الحالة** (المريض، الجلسات، الملاحظات، المرفقات، التواريخ، القسم/النوع، علاقتها بمتطلبات الكلية) + طابع زمني **`completedAt`**.
- الحالات المكتملة **تبقى محسوبة** ضمن متطلبات الكلية.
- من داخل الأرشيف: عرض مضغوط، **فتح ملف الحالة كاملاً**، وزر **«إرجاع إلى الحالات الحالية»** يعيد الحالة إلى «قيد العمل» ويُظهرها في القوائم من جديد.

### إصلاح: فتح المرفقات
- إصلاح تعذّر فتح المرفقات بالنقر: تُفتح الآن عبر **روابط Blob** بدل روابط `data:` التي تحجبها المتصفحات.
- **الصور** تُفتح في نافذة معاينة، و**ملفات PDF** وغيرها في تبويب جديد (مع تنزيل احتياطي).
- كل مرفق يعرض: الاسم، النوع/الحجم، تاريخ الإضافة، وزرّي **«فتح المرفق»** و**«حذف المرفق»**.
- حفظ بيانات أوفى للمرفقات الجديدة (`type`, `size`, `createdAt`) لتُفتح بعد إعادة التحميل.
- المرفقات القديمة بلا بيانات كافية تُظهر رسالة واضحة بدل الفشل الصامت.
- لا مساس بأي مرفقات موجودة، وتبقى مرتبطة بحالتها الصحيحة فقط.

### عام
- لا حذف لأي بيانات، ولا إعادة تعيين للتفعيل أو التجربة. مفاتيح `LocalStorage` كما هي. كاش جديد `dentpilot-student-v1.2.0`.

---

## v1.1.1 (2026) — نظام تحديث PWA آمن
- **`version.json`** + ثابت **`APP_VERSION`** داخل التطبيق (يظهران في «الإعدادات»).
- **كشف التحديث تلقائياً** عبر دورة حياة Service Worker (`updatefound` → عامل «منتظر»).
- **نافذة تحديث عربية** مع زرّي **«تحديث الآن»** و«لاحقاً».
- **تحديث متحكَّم به**: أُزيل `skipWaiting` التلقائي؛ يُفعَّل التحديث فقط عند ضغط «تحديث الآن» عبر رسالة **`SKIP_WAITING`**، ثم إعادة تحميل واحدة عند `controllerchange`.
- **`clients.claim`** + **حذف الكاش القديم** عند التفعيل (كاش جديد `dentpilot-student-v1.1.1`).
- **فحص يدوي للتحديثات** من «الإعدادات» (`registration.update()` + مقارنة `version.json`) مع رسالة حالة.
- لا مساس بأي بيانات أو تفعيل أو تجربة أو مفاتيح `LocalStorage`. يُنشر على **نفس رابط Netlify** دون إعادة تثبيت؛ البيانات والتفعيل يبقيان.

---

## v1.1.0 (2026)
### جديد: تاريخ موعد حقيقي
- إضافة **تاريخ الموعد** (حقل تاريخ فعلي) + **وقت الموعد** في إضافة/تعديل الحالة.
- **اليوم يُحسب تلقائياً** من التاريخ ويُعرض بالعربية.
- «جميع المرضى» يعرض: اليوم + التاريخ الكامل + الوقت (مثل: السبت 11 يوليو 2026 — 10:00 صباحاً).
- «المرضى حسب الأيام» يُجمّع حسب يوم التاريخ الفعلي عند توفّره، وإلا حسب «اليوم المخصّص»، وإلا «بدون يوم محدد».
- الفرز حسب التاريخ والوقت الفعليين. ملف الحالة والطباعة يعرضان تاريخ الموعد/اليوم/الوقت.
- **ترحيل آمن**: الحالات القديمة (يوم فقط بلا تاريخ) تعمل كما هي؛ يُضاف حقل `apptDate` فارغ عند الحاجة دون حذف أي بيانات. النسخ الاحتياطي/الاستعادة يشملان الحقل الجديد.

### جديد: تجربة مجانية 24 ساعة (للمستخدمين الجدد فقط)
- المستخدم الجديد غير المفعّل يحصل على **وصول كامل لمدة 24 ساعة** مع **شريط** يعرض الوقت المتبقّي.
- بعد انتهاء التجربة: **حظر** وعرض شاشة التفعيل الإلزامية برسالة انتهاء التجربة — **دون حذف أي بيانات**.
- طوابع التجربة في مفاتيح مستقلة: `dentpilot_student_trial_start` / `dentpilot_student_trial_expires`. إعادة التشغيل/التحديث لا تُعيد بدء التجربة.
- **المستخدمون المفعّلون سابقاً لا يتأثرون إطلاقاً**: يُفحص التفعيل أولاً، فلا شاشة ولا شريط ولا تُنشأ لهم طوابع تجربة، وكل بياناتهم تبقى.
- بعد إدخال كود صحيح: فتح دائم، إخفاء الشريط، والاحتفاظ بكل بيانات فترة التجربة.
- «الإعدادات» تعرض حالة الوصول: مفعل / فترة تجريبية (المتبقي) / انتهت الفترة التجريبية.
- رفع كاش الخدمة إلى `dentpilot-student-v1.1.0` (يُنشر على نفس الرابط دون إعادة تثبيت، والبيانات والتفعيل يبقيان).

---

## v1.0 — الإصدار التجاري الأول للطلاب (2026)
منتج جديد مستقل مبني على استقرار DentPilot Pro، ومخصّص لطلاب كلية الأسنان.

- لوحة طالب مبسّطة (7 أزرار) بدل لوحة العيادة.
- «جميع المرضى» كقائمة مرقّمة مدمجة + بحث متعدّد الحقول.
- «المرضى حسب الأيام» مع جدول أسبوعي سريري قابل للتعديل من الإعدادات.
- «متطلبات الكلية» مع احتساب تلقائي للمكتمل وأشرطة تقدّم.
- «إضافة حالة» بمصطلحات طلابية، وملف حالة مبسّط (بيانات/جلسات/ملاحظات/مرفقات/طباعة).
- الجلسات بلا حقول مالية (تركيز سريري).
- نسخ احتياطي/استعادة لبيانات الطالب فقط.
- **تخزين منفصل تماماً** عن Pro (مفاتيح مستقلة) — لا يمسّ بيانات Pro.
- **PWA منفصل**: اسم ومانيفست وكاش (`dentpilot-student-v1.0.0`) مستقلة.
- **تفعيل منفصل**: `PRODUCT_ID = DENTPILOT_STUDENT` — أكواد Pro لا تُفعّل Student والعكس.

### حدود معروفة
- المرفقات محلية (base64) ضمن حدود سعة المتصفح (~5MB)؛ تُصغَّر الصور تلقائياً.
- الطباعة عبر مربّع الطباعة (اختر «حفظ كـ PDF» عند الحاجة).
- التفعيل من طرف العميل قابل للكسر نظرياً؛ التعتيم يرفع الحاجز فقط.
- مسح بيانات المتصفح قد يُنشئ معرّف جهاز جديداً ويستلزم إعادة تفعيل.
